<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>掲示板 サンプル</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css">
    <link rel="stylesheet" href="public/css/style.css">
</head>
<body>
    <div class="root">
        <header class="header">
            <h1 class="header-title">掲示板 サンプル</h1>
            <p><a href="/">ホームへ戻る</a></p>
        </header>
        <main class="main">
            <section>
                <div>
                    <form action="/search" method="get" style="display: flex;">
                        <input type="text" name="q" placeholder="タイトル検索" value=<%= h(q) %>>
                        <input type="submit" value="検索">
                    </form>
                </div>
            </section>
            <br>
            <section class="sections">
                <h2>「<%= h(q) %>」の検索結果</h2>
                <p><%= threads_length > 0 ? "#{threads_length}件のスレッドが見つかりました" : "該当するスレッドは見つかりませんでした" %></p>
                <% threads.each do |thread| %>
                <div class="thread">
                    <a href="/view?id=<%= h(thread['id']) %>" class="thread-link">
                        <div class="thread-content">
                            <span class="thread-title">
                                <%= h(thread['title']) %>
                            </span>
                            <span class="thread-datetime">
                                <%= h(Time.parse(thread['datetime']).strftime('%Y/%m/%d %H:%M')) %>
                            </span>
                        </div>
                    </a>
                </div>
                <% end %>
            </section>
        </main>
    </div>
</body>
</html>